# 命令说明

所有命令都在项目根目录执行（有 `package.json` 的那一层）。第一次使用先安装依赖。

```bash
npm install
```

---

## 1. 克隆框架（新开游戏时）

从 GitHub 拉一份框架到本地：

```bash
git clone https://github.com/ZhangJieFcy/AIGameFramework.git 你的游戏名
cd 你的游戏名
npm install
```

把 `你的游戏名` 换成文件夹名，例如 `my-idle-game`。

---

## 2. `npm run dev` — 本地开发

启动开发服务器，改代码会自动刷新。

```bash
npm run dev
```

终端里会打印一个地址（一般是 `http://localhost:5173`），用浏览器打开。

注意：不要双击 `index.html` 用 `file://` 打开，配置是 `fetch` 读的，会黑屏或加载失败。

---

## 3. `npm run build:web` — 打 Web 包

生成网页版发布文件，输出目录：`dist-web/`。

```bash
npm run build:web
```

预览这个包：

```bash
npx vite preview --outDir dist-web
```

或用任意本地 HTTP 服务打开 `dist-web`，不要双击 html。

---

## 4. `npm run build` — 默认构建

和 `build:web` 类似，但输出到 `dist/`（Vite 默认目录）。日常发布 Web 请用 `build:web`。

```bash
npm run build
```

---

## 5. `npm run preview` — 预览默认构建

预览 `dist/`（先跑过 `npm run build`）。

```bash
npm run preview
```

---

## 6. `npm run build:wechat` — 打微信小游戏包

先打 Web 包，再生成微信入口和适配器。输出目录：`dist-wechat/`。

```bash
npm run build:wechat
```

然后用微信开发者工具导入 `dist-wechat/`。控制台应出现 `runtime ready` 一类日志。

---

## 7. `npm run build:douyin` — 打抖音小游戏包

流程与微信相同，接口换成抖音。输出目录：`dist-douyin/`。

```bash
npm run build:douyin
```

然后用抖音开发者工具导入 `dist-douyin/`。

---

## 命令对照

| 命令 | 作用 | 产物 |
|------|------|------|
| `npm install` | 安装依赖，第一次必做 | `node_modules/` |
| `npm run dev` | 本地开发、热更新 | 无，浏览器访问本地地址 |
| `npm run build:web` | Web 发布包 | `dist-web/` |
| `npm run build` | 默认构建 | `dist/` |
| `npm run preview` | 预览 `dist/` | 无 |
| `npm run build:wechat` | 微信小游戏包 | `dist-wechat/` |
| `npm run build:douyin` | 抖音小游戏包 | `dist-douyin/` |

---

## 做游戏时改哪里

- 玩法：`src/game/MyGame.ts`
- 配置：`public/config/`
- 框架（一般不用改）：`src/framework/`
